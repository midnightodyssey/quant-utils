# quant-lab

Educational Python toolkit for quantitative trading research: vectorised backtesting, risk metrics, momentum strategies, position sizing, Black-Scholes options pricing, **pairs cointegration**, and a **market-making simulator**.

```bash
pip install -e ".[dev]"
pytest -q
```

Synthetic sample data only — no proprietary market feeds or secrets.
